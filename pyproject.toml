[project]
name = "dbos-postgres-benchmark"
version = "0.1.0"
description = "Postgres and DBOS benchmarks"
requires-python = ">=3.14"
dependencies = [
    "asyncpg>=0.29",
    "dbos==2.30.0a10",
]

[dependency-groups]
dev = [
    "black>=26.3.1",
    "isort>=8.0.1",
]
